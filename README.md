LiquorAndroidProject
============
# 一、关于二维码扫描相机请求的权限
1. uses-permission android:name="android.permission.CAMERA"
1. uses-feature android:name="android.hardware.camera"
1. uses-feature android:name="android.hardware.camera.autofocus"